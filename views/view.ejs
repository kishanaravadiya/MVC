  <table border="1">
    <tr>
        <th>Name</th>
        <th>Phone</th>
        <th>Email</th>
        <th>Password</th>
    </tr>
    <% for(i in record) { %>
        <tr>
            <td><%= record[i].S_name %></td>
            <td><%= record[i].S_phone %></td>
            <td><%= record[i].S_email %></td>
            <td><%= record[i].S_password %></td>
            <td><a href="/admin/DeletData/<%= record[i].id %>">Delete</a></td>
            <td><a href="/admin/EditData/<%= record[i].id %>">Edit</a></td>
        </tr>
    <% } %>
</table>

<a href="/admin">InsertData</a>